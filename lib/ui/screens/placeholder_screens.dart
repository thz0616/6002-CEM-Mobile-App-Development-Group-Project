import 'package:flutter/material.dart';

class AllergicScreen extends StatelessWidget {
  const AllergicScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Allergen Flow')),
      body: const Center(child: Text('Allergen scanner and setup will be implemented here.')),
    );
  }
}



